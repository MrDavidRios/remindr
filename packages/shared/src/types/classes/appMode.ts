// eslint-disable-next-line import/prefer-default-export
export enum AppMode {
  Offline = 'offline',
  Online = 'online',
  LoginScreen = 'login-screen',
}

export const appModeLabels = ['Offline', 'Online', 'Login Screen'];
