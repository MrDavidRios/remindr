export type ThemeColors = {
  text: {
    primary: string;
    secondary: string;
  };
  surface: {
    primary: string;
    primaryDark: string;
    secondary: string;
    active: string;
  };
  imgFilter: string;
  svgFilter: string;
  boxShadow: string;
};
